## README

here we have two versions of the script

v1 -> first version just to make it works quickly
v2 -> same code but refactored to be OO



Ruby version: 4.0.1

Gems used:
- Rspec

## Running the project

to Run V1 use: `ruby lib/main_v1.rb`
to Run V2 use: `ruby lib/main_v2.rb`